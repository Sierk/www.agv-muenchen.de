<% sitemap.resources.each do |resource| %>
<% if resource.url !~ /\.(css|js|eot|svg|woff|otf|ttf|png|jpg|gif|ico)$/ && (resource.data.seo.present? ? resource.data.seo.priority.to_f > 0 : true) %>
<%= URI.join(data.site.url, resource.url) %>

<% end %>
<% end %>
